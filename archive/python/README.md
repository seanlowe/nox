To tinker with the python version of **nox**, navigate to the python directory and run:
```
poetry install
```

Then to run:
```
python3 index.py
```

*note:* see Poetry's github page for installation.